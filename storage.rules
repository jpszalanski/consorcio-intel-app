rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Only Admin can write (upload/delete)
    match /{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth.token.admin == true;
    }
  }
}
